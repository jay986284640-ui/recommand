# 规格质量检查清单:优惠推荐 Agent (Promo Recommend Agent)

**目的**:在进入规划阶段前,验证规格说明的完整性与质量。
**创建日期**: 2026-06-12
**对应功能**: [spec.md](../spec.md)

## 内容质量

- [x] 没有实现细节(语言、框架、API)
- [x] 聚焦于用户价值与业务需求
- [x] 面向非技术干系人
- [x] 所有必填章节已完成

## 需求完整性

- [x] 没有遗留的 [NEEDS CLARIFICATION] 标记
- [x] 需求可测且无歧义
- [x] 成功标准可度量
- [x] 成功标准与具体技术无关(无实现细节)
- [x] 所有验收场景已定义
- [x] 边界场景已识别
- [x] 范围已清晰界定
- [x] 依赖与假设已识别

## 功能就绪度

- [x] 每条功能性需求都有清晰的验收条件
- [x] 用户场景覆盖主要流程
- [x] 功能满足成功标准中定义的可度量结果
- [x] 规格中没有泄漏实现细节

## 验证摘要

| 维度 | 状态 | 说明 |
|------|------|------|
| 范围明确 | ✅ | 明确给出第一期在范围内/外的事项,并区分了 P1~P8 的优先级 |
| 领域上下文 | ✅ | 明确本地优惠的四类内容形态(美团门店券 / 自拓展门店券 / 本地优惠买单 / 外部券)与"收银台"支付模型,以及积分商城的"积分扣减"模型;两者实现层无共享 |
| 主/子 Agent 划分 | ✅ | 第一期仅一个子 Agent(本地优惠),**四层**内部工具(推荐 / 下单 / 券包管理 / 排除与偏好);第二期才引入积分商城子 Agent(满足 YAGNI) |
| 对话式排除 + 跨会话记忆 | ✅ | 用户故事 5(优先级 P5),FR-018(临时排除 + 持久化追问)、FR-019(显式 180 天 / 隐式 30 天、隐式默认关闭)、FR-019b(立即清除);由主 Agent 走"持久化追问"对话流,LP 只读 Preference |
| 联想+语义召回 | ✅ | FR-017:LLM 扩词 + ES 关键词+向量双路召回 + 去重;幻觉护栏:商品必须来自 ES 召回结果 |
| 排序键集合 | ✅ | FR-030 + OQ-1 已解决:第一期支持 `distance / popularity / price / model` 四种;按有效期推迟到第二期 |
| **券包管理** | ✅ | 用户故事 7(优先级 P3)+ FR-024(三个工具)+ FR-025(与下单工具衔接)+ FR-026(快过期引导);数据源是后端用户资产查询服务,不拥有数据 |
| **上下文压缩** | ✅ | 用户故事 8(优先级 P6)+ FR-054(轮次/字符阈值触发,降级策略,无 PII)+ 实体 Session Summary;主 Agent 自有能力,压缩对用户透明 |
| 失败交易归因 | ✅ | 用户故事 6(优先级 P7)+ FR-053(异步、不阻塞、无 PII)+ 实体 Conversation Trace |
| **多端共享会话** | ✅ | FR-003 已扩展为"per-user 一份 session state,跨多端共享";FR-023b 新增"支付前草稿互斥校验";A-017 锁定同步机制 |
| **外部服务降级** | ✅ | FR-012b(ES 降级到缓存候选)+ FR-014b(推荐服务降级到热度榜)+ A-018(降级 SLO 假设);降级阶梯清晰 |
| **数据主体权利** | ✅ | FR-019b(立即清除偏好)+ A-019(数据保留与合规边界,PIPL、回溯聚合脱敏 6 个月) |
| **限流 / 防刷** | ✅ | FR-055(per-user 50 次/分钟,敏感操作更严格,超限明确提示);与多端共享会话一致 |
| **新用户回退** | ✅ | A-009 + FR-014c:走"全站热度榜",**不带角标**,阈值由 plan 阶段定 |
| 交互方式 | ✅ | 单独章节"设计——交互方式"覆盖了 H5↔主 Agent、主 Agent↔子 Agent、跨垂直协同、排除追问、上下文压缩、降级等 |
| 可扩展性(第二期积分商城) | ✅ | FR-040~FR-042 锁定契约,SC-006 给出可验证门;四层内部工具的分层为第二期抽离/共享/复制提供清晰边界 |
| 测试性 | ✅ | 9 条成功标准全部可被自动化/手工验证;用户故事 5~8 都给出了独立可测试的验收场景 |
| 澄清记录 | ✅ | 5 个澄清已写入 `## Clarifications` 小节(Session 2026-06-12),并下沉到对应 FR / 假设 |
| 中文 | ✅ | 全文中文(技术名词与英文 Envelope 字段保留英文) |

## 备注

- 检查项在完成后请改为 `[x]`。
- 未完成项需要在执行 `/speckit-clarify` 或 `/speckit-plan` 前更新规格。
- 用户故事以"独立可测试"为前提进行排序(共 8 个):
  - **P1** = US 1:对话式本地优惠发现(可独立交付)
  - **P2** = US 2:Agent 协助下单(依赖 P1)
  - **P3** = US 7:券包管理(依赖 P2 产出的"已领券"数据)
  - **P4** = US 3:对话式重排(润色 P1)
  - **P5** = US 5:排除 + 跨会话偏好记忆(润色 P1,需新基础设施)
  - **P6** = US 8:上下文压缩(主 Agent 自有能力,长会话支撑)
  - **P7** = US 6:失败交易归因(非用户面向,需离线消费)
  - **P8** = US 4:第二期扩展点(契约级约束)
- "待确认问题(OQ-1、OQ-3)已解决;OQ-2 部分收敛"。
- 5 个澄清问题(Q1~Q5)已答完并写入 spec.md 的 `## Clarifications` 小节;**所有 5 个都是高/中-高影响,无法推迟到 plan 阶段**。
